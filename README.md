Take the methods get_answer, get_classifaction, self_refine, chain_of_thought, and call_model_chat_completions to a .py file.
With the given code to run the real data important these 3 methods from the .py file made.
Then inside the build_answers call the methods to generate the output for the JSON file 
